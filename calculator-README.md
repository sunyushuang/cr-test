# 现代计算器

一个功能丰富、界面美观的Web计算器，支持基础运算和科学计算。

## 功能特性

### 基础功能
- ✅ 四则运算（加、减、乘、除）
- ✅ 小数点运算
- ✅ 正负号切换
- ✅ 清除功能（C/CE）
- ✅ 退格删除
- ✅ 历史记录显示

### 科学计算
- ✅ 三角函数（sin, cos, tan）
- ✅ 对数函数（log, ln）
- ✅ 幂运算（x^y）
- ✅ 平方根（√）
- ✅ 绝对值（|x|）

### 用户体验
- 🎨 现代化玻璃拟态设计
- 📱 响应式布局，支持移动设备
- ⌨️ 键盘快捷键支持
- 🎯 错误处理和提示
- ✨ 流畅的动画效果

## 使用方法

### 启动计算器
1. 在浏览器中打开 `index.html` 文件
2. 计算器界面将自动加载

### 基础操作
- **数字输入**：点击数字按钮或使用键盘数字键
- **运算符**：点击 +、-、×、÷ 按钮或使用键盘对应键
- **等号计算**：点击 = 按钮或按 Enter 键
- **小数点**：点击 . 按钮或按键盘句号键
- **清除**：
  - C：清除所有内容
  - CE：清除当前输入
  - ⌫：删除最后一位数字

### 键盘快捷键
- `0-9`：输入数字
- `+, -, *, /`：运算符
- `Enter` 或 `=`：计算结果
- `Backspace`：删除最后一位
- `Escape` 或 `C`：清除所有
- `.` 或 `,`：小数点

### 科学计算
1. 点击"科学"按钮展开科学计算功能
2. 输入数字后点击相应的科学函数按钮
3. 支持的函数：
   - `sin`：正弦函数（输入角度）
   - `cos`：余弦函数（输入角度）
   - `tan`：正切函数（输入角度）
   - `log`：常用对数（以10为底）
   - `ln`：自然对数（以e为底）
   - `x^y`：幂运算
   - `√`：平方根
   - `|x|`：绝对值

### 错误处理
- 除零错误：显示"除数不能为零"
- 对数错误：负数或零的对数运算
- 平方根错误：负数的平方根
- 结果超出范围：显示科学计数法或错误提示
- 错误状态会在3秒后自动清除

## 技术特点

- **纯前端实现**：HTML + CSS + JavaScript
- **面向对象设计**：使用ES6类组织代码
- **现代CSS**：使用Grid布局和Flexbox
- **玻璃拟态效果**：backdrop-filter和半透明设计
- **平滑动画**：CSS transitions和keyframes
- **响应式设计**：适配不同屏幕尺寸

## 浏览器兼容性

- Chrome 88+
- Firefox 94+
- Safari 14+
- Edge 88+

## 文件结构

```
calculator/
├── index.html      # 主页面
├── style.css       # 样式文件
├── script.js       # JavaScript逻辑
└── README.md       # 说明文档
```

## 开发说明

计算器采用模块化设计，主要包含以下组件：

1. **Calculator类**：核心计算逻辑
2. **显示系统**：数字格式化和历史记录
3. **输入处理**：鼠标和键盘事件
4. **错误处理**：异常情况的用户友好提示
5. **科学计算**：扩展的数学函数支持

享受使用这个现代化的计算器吧！🧮✨